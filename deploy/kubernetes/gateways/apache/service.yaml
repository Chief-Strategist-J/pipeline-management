apiVersion: v1
kind: Service
metadata:
  name: gateway-apache
  namespace: gateway
spec:
  ports:
    - port: 443
      targetPort: 8443
      protocol: TCP
      name: https
  selector:
    app: gateway-apache
  type: LoadBalancer
