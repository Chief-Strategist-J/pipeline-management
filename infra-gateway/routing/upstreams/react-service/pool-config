# Upstream pool configuration for react-service static frontend
targets:
  - "10.244.20.1:3000" # React dev app runs on port 3000, or Nginx-served static build at port 80

load_balancing: round_robin

keepalive:
  connections: 32
  timeout: 60s
